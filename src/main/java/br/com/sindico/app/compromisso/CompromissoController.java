package br.com.sindico.app.compromisso;

/**
 * Rota MVC /compromissos desativada — React + /api/compromissos cuida de tudo.
 * Mantido apenas para não quebrar compilação.
 */
@org.springframework.stereotype.Controller
@org.springframework.web.bind.annotation.RequestMapping("/compromissos-legado")
public class CompromissoController {
    // Sem rotas ativas.
}

